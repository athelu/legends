<form class="{{cssClass}} flexcol" autocomplete="off">
  
  {{!-- Header --}}
  <header class="sheet-header">
    <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" height="100" width="100"/>
    <div class="header-info">
      <div class="info-line">
        <label>Name:</label>
        <input name="name" type="text" value="{{actor.name}}" placeholder="Character Name"/>
      </div>
      <div class="info-line">
        <label>Background:</label>
        <input type="text" name="system.biography.background" value="{{system.biography.background}}" placeholder="Background"/>
      </div>
      <div class="info-line">
        <label>Race:</label>
        <input type="text" name="system.biography.ancestry" value="{{system.biography.ancestry}}" placeholder="Ancestry"/>
      </div>
      <div class="info-line">
        <label>Tier:</label>
        <span class="tier-display">{{tier.current}}</span>
        <label class="xp-label">Total XP:</label>
        <input type="number" name="system.tier.xp" value="{{tier.xp}}" data-dtype="Number" class="xp-input"/>
        <label class="xp-label">Unspent XP:</label>
        <input type="number" name="system.tier.unspent" value="0" data-dtype="Number" class="xp-input"/>
      </div>
    </div>
  </header>

  {{!-- Sheet Tab Navigation --}}
  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-tab="main">Main</a>
    <a class="item" data-tab="weapons">Weapons</a>
    <a class="item" data-tab="actions">Actions</a>
    {{#if system.energy}}
    <a class="item" data-tab="magic">Magic</a>
    {{/if}}
    <a class="item" data-tab="features">Features</a>
    <a class="item" data-tab="effects">Effects</a>
    <a class="item" data-tab="equipment">Equipment</a>
    <a class="item" data-tab="biography">Biography</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class="sheet-body">
    
    {{!-- Main Tab --}}
    <div class="tab main" data-group="primary" data-tab="main">
      <div class="main-layout">
        {{!-- Left Column: Skills --}}
        <div class="skills-column">
          <h3>Skills</h3>
          <div class="skills-list">
            {{#each skillList}}
            <div class="skill-row" data-skill="{{key}}">
              <label class="skill-name">{{label}}</label>
              <span class="skill-attr">&lt;{{attr}}&gt;</span>
              <input type="number" name="system.skills.{{key}}" value="{{base}}" data-dtype="Number" class="skill-base"/>
              <span class="skill-bonus" title="{{breakdown}}">{{#if (gt bonus 0)}}+{{/if}}{{bonus}}</span>
              <span class="skill-total" title="Total = Base + Bonus\nBase: {{base}}\nBonus: {{bonus}}\n{{breakdown}}">{{total}}</span>
              <button type="button" data-action="skillRoll" data-skill="{{key}}" title="Roll {{label}}">
                <i class="fas fa-dice-d8"></i>
              </button>
            </div>
            {{/each}}
          </div>
        </div>

        {{!-- Right Column: Attributes & Resources --}}
        <div class="stats-column">
          <h3>Attributes</h3>
          <div class="attributes-list">
            {{#each attributeList as |attr|}}
            <div class="attribute-row" data-attribute="{{attr.key}}">
              <label>{{attr.label}}</label>
              <input type="number" name="system.attributes.{{attr.key}}.value" value="{{attr.base}}" data-dtype="Number" class="attribute-value"/>
              <span class="attribute-bonus" title="{{attr.breakdown}}">{{#if (gt attr.bonus 0)}}+{{/if}}{{attr.bonus}}</span>
              <span class="attribute-total" title="Total = Base + Bonus\nBase: {{attr.base}}\nBonus: {{attr.bonus}}\n{{attr.breakdown}}">{{attr.total}}</span>
            </div>
            {{/each}}
          </div>

          <h3>Resources</h3>
          <div class="resources-section">
            {{!-- HP --}}
            <div class="resource-row hp-row">
              <label>HP</label>
              <input type="number" name="system.hp.value" value="{{system.hp.value}}" data-dtype="Number" class="resource-current"/>
              <span class="sep">/</span>
              <span class="resource-max">{{system.hp.max}}</span>
              <div class="temp-hp-section">
                <label class="temp-label">Temp:</label>
                <input type="number" name="system.hp.temp" value="{{system.hp.temp}}" data-dtype="Number" class="temp-hp-input" placeholder="0"/>
              </div>
            </div>

            {{!-- Luck --}}
            <div class="resource-row luck-row">
              <label>Luck</label>
              <input type="number" name="system.luck.current" value="{{system.luck.current}}" data-dtype="Number" class="resource-current"/>
              <span class="sep">/</span>
              <span class="resource-max">{{system.luck.max}}</span>
            </div>

            {{!-- Energy (if caster) --}}
            {{#if system.energy}}
            <div class="resource-row energy-row">
              <label>Energy</label>
              <input type="number" name="system.energy.value" value="{{system.energy.value}}" data-dtype="Number" class="resource-current"/>
              <span class="sep">/</span>
              <span class="resource-max">{{system.energy.max}}</span>
            </div>
            {{/if}}

            {{!-- DR --}}
            <div class="resource-row dr-row">
              <label>DR</label>
              <span class="dr-value">{{system.dr.total}}</span>
            </div>
          </div>

          {{!-- Rest Buttons --}}
          <div class="rest-buttons">
            <button type="button" data-action="shortRest" title="Short Rest (10 minutes): Regain Con HP + 1 Luck">
              <i class="fas fa-moon"></i> Short Rest
            </button>
            <button type="button" data-action="longRest" title="Long Rest (8 hours): Regain (Con × 4) HP, restore all Luck & Energy">
              <i class="fas fa-bed"></i> Long Rest
            </button>
          </div>
        </div>
      </div>
    </div>

    {{!-- Weapons Tab   --}}
    <div class="tab weapons" data-group="primary" data-tab="weapons">
      <div class="inventory-header">
        <h3>Weapons</h3>
        <button type="button" data-action="openCompendium" data-type="weapon" title="Add Weapon">
          <i class="fas fa-plus"></i> Add
        </button>
      </div>
      
      <ol class="items-list weapon-list">
        <li class="items-header flexrow">
          <div class="item-name">Name</div>
          <div class="item-attack">Attack</div>
          <div class="item-damage">Damage</div>
          <div class="item-controls"></div>
        </li>
        
        {{#each weapons as |item id|}}
        <li class="item flexrow weapon-item" data-item-id="{{item._id}}">
          <div class="item-image">
            <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
          </div>
          
          <h4 class="item-name">{{item.name}}</h4>
          
          <div class="item-attack">
            {{!-- Show attack buttons based on attack modes --}}
            {{#each item.system.attackModes as |mode modeIndex|}}
              {{#if (eq mode.type "melee")}}
                <button type="button" 
                        class="weapon-attack-btn melee-attack" data-action="weaponAttack" 
                        data-mode-index="{{modeIndex}}"
                        title="Melee Attack">
                  <i class="fas fa-hand-fist"></i>
                </button>
              {{/if}}
              {{#if (or (eq mode.type "ranged") (eq mode.type "thrown"))}}
                <button type="button" 
                        class="weapon-attack-btn ranged-attack" data-action="weaponAttack" 
                        data-mode-index="{{modeIndex}}"
                        title="{{#if (eq mode.type 'thrown')}}Throw{{else}}Ranged Attack{{/if}}">
                  <i class="fas fa-bullseye"></i>
                </button>
              {{/if}}
            {{/each}}
          </div>
          
          <div class="item-damage">
            {{item.system.damage.base}} {{item.system.damage.type}}
          </div>
          
          <div class="item-controls">
            <a class="item-control {{#if item.system.equipped}}equipped{{/if}}" data-action="itemEquip"
              title="{{#if item.system.equipped}}Equipped{{else}}Not Equipped{{/if}}">
              <i class="{{#if item.system.equipped}}fas{{else}}far{{/if}} fa-circle"></i>
            </a>
            <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
            <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
          </div>
        </li>
        {{/each}}
      </ol>
    </div>

    {{!-- Actions Tab --}}
    <div class="tab actions" data-group="primary" data-tab="actions">
      <div class="inventory-header">
        <h3>Actions & Reactions</h3>
        <button type="button" data-action="openCompendium" data-type="action" title="Add from Compendium">
          <i class="fas fa-book"></i> Add from Compendium
        </button>
      </div>

      {{!-- Combat Actions --}}
      {{#if (hasItems combatActions)}}
      <div class="action-section">
        <h4>Combat Actions</h4>
        <ol class="items-list action-list">
          {{#each combatActions as |item id|}}
          <li class="item flexrow" data-item-id="{{item._id}}">
            <div class="item-image">
              <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
            </div>
            <h4 class="item-name">{{item.name}}</h4>
            <div class="item-detail action-cost">
              {{#if item.system.actionCost}}[{{item.system.actionCost}}]{{/if}}
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
              <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
              <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
            </div>
          </li>
          {{/each}}
        </ol>
      </div>
      {{/if}}

      {{!-- Movement Actions --}}
      {{#if (hasItems movementActions)}}
      <div class="action-section">
        <h4>Movement Actions</h4>
        <ol class="items-list action-list">
          {{#each movementActions as |item id|}}
          <li class="item flexrow" data-item-id="{{item._id}}">
            <div class="item-image">
              <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
            </div>
            <h4 class="item-name">{{item.name}}</h4>
            <div class="item-detail action-cost">
              {{#if item.system.actionCost}}[{{item.system.actionCost}}]{{/if}}
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
              <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
              <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
            </div>
          </li>
          {{/each}}
        </ol>
      </div>
      {{/if}}

      {{!-- Activate Actions --}}
      {{#if (hasItems activateActions)}}
      <div class="action-section">
        <h4>Activate Actions</h4>
        <ol class="items-list action-list">
          {{#each activateActions as |item id|}}
          <li class="item flexrow" data-item-id="{{item._id}}">
            <div class="item-image">
              <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
            </div>
            <h4 class="item-name">{{item.name}}</h4>
            <div class="item-detail action-cost">
              {{#if item.system.actionCost}}[{{item.system.actionCost}}]{{/if}}
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
              <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
              <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
            </div>
          </li>
          {{/each}}
        </ol>
      </div>
      {{/if}}

      {{!-- Interact Actions --}}
      {{#if (hasItems interactActions)}}
      <div class="action-section">
        <h4>Interact Actions</h4>
        <ol class="items-list action-list">
          {{#each interactActions as |item id|}}
          <li class="item flexrow" data-item-id="{{item._id}}">
            <div class="item-image">
              <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
            </div>
            <h4 class="item-name">{{item.name}}</h4>
            <div class="item-detail action-cost">
              {{#if item.system.actionCost}}[{{item.system.actionCost}}]{{/if}}
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
              <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
              <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
            </div>
          </li>
          {{/each}}
        </ol>
      </div>
      {{/if}}

      {{!-- Free Actions --}}
      {{#if (hasItems freeActions)}}
      <div class="action-section">
        <h4>Free Actions</h4>
        <ol class="items-list action-list">
          {{#each freeActions as |item id|}}
          <li class="item flexrow" data-item-id="{{item._id}}">
            <div class="item-image">
              <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
            </div>
            <h4 class="item-name">{{item.name}}</h4>
            <div class="item-detail action-cost">
              {{#if item.system.actionCost}}[{{item.system.actionCost}}]{{/if}}
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
              <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
              <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
            </div>
          </li>
          {{/each}}
        </ol>
      </div>
      {{/if}}

      {{!-- Reactions --}}
      {{#if (hasItems reactions)}}
      <div class="action-section">
        <h4>Reactions</h4>
        <ol class="items-list action-list">
          {{#each reactions as |item id|}}
          <li class="item flexrow" data-item-id="{{item._id}}">
            <div class="item-image">
              <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
            </div>
            <h4 class="item-name">{{item.name}}</h4>
            <div class="item-detail action-trigger">
              {{#if item.system.trigger}}<small>{{item.system.trigger}}</small>{{/if}}
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
              <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
              <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
            </div>
          </li>
          {{/each}}
        </ol>
      </div>
      {{/if}}
    </div>

    {{!-- Magic Tab --}}
    {{#if system.energy}}
    <div class="tab magic" data-group="primary" data-tab="magic">
      
      {{!-- Magical Trait Setup Section --}}
      {{#if system.magicalTrait.type}}
      <div class="magical-trait-section">
        <h3>Magical Powers</h3>
        
        {{#if system.magicalTrait.isSetup}}
          {{!-- Magical trait is configured - show stats --}}
          <div class="magic-stats-display">
            {{!-- Combined Potentials and Mastery Display --}}
            <div class="energy-header">
              <h4>Magical Energies</h4>
              {{#if system.castingStat.displayLabel}}
              <div class="casting-stat-inline">
                <label>Casting:</label>
                <span>{{system.castingStat.displayLabel}} {{system.castingStat.displayValue}}</span>
              </div>
              {{/if}}
              {{#if system.channelDivinity.max}}
              <div class="channel-uses">
                <label>Channel Divinity:</label>
                <input type="number" name="system.channelDivinity.current" 
                       value="{{system.channelDivinity.current}}" data-dtype="Number" 
                       class="uses-input" min="0" max="{{system.channelDivinity.max}}"/>
                <span class="sep">/</span>
                <span class="max-value">{{system.channelDivinity.max}}</span>
              </div>
              {{/if}}
            </div>
            
            <div class="energy-potentials-grid">
              {{#each system.potentials as |potential key|}}
              <div class="energy-row">
                <label class="energy-label">{{potential.label}}</label>
                <div class="energy-values">
                  <input type="number" name="system.mastery.{{key}}.value" 
                         value="{{lookup ../system.mastery key 'value'}}" data-dtype="Number" 
                         min="0" max="10" class="energy-input" title="Mastery"
                         placeholder="0"/>
                  <span class="separator">/</span>
                  <input type="number" name="system.potentials.{{key}}.value" 
                         value="{{potential.value}}" data-dtype="Number" 
                         min="0" max="10" class="energy-input" title="Potential"
                         placeholder="0"/>
                </div>
              </div>
              {{/each}}
            </div>
          </div>
        {{else}}
          {{!-- Magical trait needs setup --}}
          <div class="magic-setup-required">
            <div class="setup-info">
              <h4>🌟 Magical Trait Setup Required</h4>
              <p>You have selected a magical trait that needs configuration.</p>
              
              <div class="trait-preview">
                <strong>Primary Trait:</strong> 
                <span class="trait-name">{{magicalTraitDisplayName}}</span>
              </div>
              
              {{#if hasGiftedMage}}
              <div class="modifier-badge gifted">
                ✨ <strong>Gifted Mage</strong> - Will roll 9d8 and drop lowest
              </div>
              {{/if}}
              
              {{#if hasBalancedChanneler}}
              <div class="modifier-badge balanced">
                ⚖️ <strong>Balanced Channeler</strong> - Will use fixed array
              </div>
              {{/if}}
              
              <p class="setup-instructions">
                Click the button below to begin the guided setup process. 
                You will choose your affinities, roll for potentials, and configure your magical abilities.
              </p>
              
              <button type="button" class="setup-magical-traits-btn" data-action="setupMagicalTraits">
                <i class="fas fa-magic"></i> Begin Magical Trait Setup
              </button>
            </div>
          </div>
        {{/if}}
      </div>
      {{/if}}
      
      <div class="inventory-header">
        <h3>Weaves</h3>
        <button type="button" data-action="openCompendium" data-type="weave" title="Add from Compendium">
          <i class="fas fa-atlas"></i> Add Weave
        </button>
      </div>
      
      {{!-- Weave Energies --}}
      <div class="weave-energies">
        <h4>Weaving Proficiencies</h4>
        <div class="energy-grid">
          {{#each system.energies as |energy key|}}
          <div class="energy-item" data-energy="{{key}}">
            <label>{{energy.label}}</label>
            <input type="number" name="system.energies.{{key}}.value" value="{{energy.value}}" data-dtype="Number" min="0" max="10"/>
          </div>
          {{/each}}
        </div>
      </div>

      {{!-- Weaves List --}}
      <ol class="items-list weave-list">
        <li class="items-header flexrow">
          <div class="item-image">Icon</div>
          <div class="item-name">Name</div>
          <div class="energy-display">Primary</div>
          <div class="energy-display">Supporting</div>
          <div class="item-detail">Total</div>
          <div class="item-controls">Actions</div>
        </li>
        {{#each weaves as |item id|}}
        <li class="item flexrow collapsible" data-item-id="{{item._id}}">
          <div class="item-image">
            <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
          </div>
          <h4 class="item-name expandable" data-action="expandItem">
            <i class="fas fa-caret-right expand-caret"></i>
            {{item.name}}
          </h4>
          <div class="energy-display">
            {{#if item.system.energyCost.primary.type}}
              <img src="systems/legends/ui/energy-{{item.system.energyCost.primary.type}}.png" 
                   width="16" height="16" title="{{capitalize item.system.energyCost.primary.type}}"/>
              {{item.system.energyCost.primary.cost}}
            {{else}}—{{/if}}
          </div>
          <div class="energy-display">
            {{#if item.system.energyCost.supporting.type}}
              <img src="systems/legends/ui/energy-{{item.system.energyCost.supporting.type}}.png" 
                   width="16" height="16" title="{{capitalize item.system.energyCost.supporting.type}}"/>
              {{item.system.energyCost.supporting.cost}}
            {{else}}—{{/if}}
          </div>
          <div class="item-detail">
            {{#if item.system.totalEnergyCost}}
              {{item.system.totalEnergyCost}}
            {{else}}
              {{add item.system.energyCost.primary.cost item.system.energyCost.supporting.cost}}
            {{/if}}
          </div>
          <div class="item-controls">
            <a class="item-control" data-action="itemRoll" title="Cast Weave"><i class="fas fa-dice-d8"></i></a>
            <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
            <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
            <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
          </div>
          
          {{!-- Expanded Details --}}
          <div class="item-details" style="display: none;">
            <div class="weave-expanded">
              {{#if item.system.energyCost.primary.type}}
              <div class="detail-row">
                <strong>Primary Energy:</strong> 
                {{capitalize item.system.energyCost.primary.type}} {{item.system.energyCost.primary.cost}}
                {{#if item.system.energyCost.primary.description}}({{item.system.energyCost.primary.description}}){{/if}}
              </div>
              {{/if}}
              
              {{#if item.system.energyCost.supporting.type}}
              <div class="detail-row">
                <strong>Supporting Energy:</strong> 
                {{capitalize item.system.energyCost.supporting.type}} {{item.system.energyCost.supporting.cost}}
                {{#if item.system.energyCost.supporting.description}}({{item.system.energyCost.supporting.description}}){{/if}}
              </div>
              {{/if}}
              
              {{#if item.system.range}}
              <div class="detail-row"><strong>Range:</strong> {{item.system.range}}</div>
              {{/if}}
              
              {{#if item.system.duration}}
              <div class="detail-row"><strong>Duration:</strong> {{item.system.duration}}</div>
              {{/if}}
              
              <div class="detail-row">
                <strong>Action Cost:</strong>
                {{#if item.system.weaveType}}
                  {{#if (eq item.system.weaveType "simple")}}Simple weave (1 action){{/if}}
                  {{#if (eq item.system.weaveType "complex")}}Complex weave (2 actions){{/if}}
                  {{#if (eq item.system.weaveType "ritual")}}Ritual (10 minutes){{/if}}
                {{else}}
                  {{#if item.system.actionCost}}{{item.system.actionCost}} action{{#if (gt item.system.actionCost 1)}}s{{/if}}{{else}}1 action{{/if}}
                {{/if}}
              </div>
              
              {{#if item.system.description.value}}
              <div class="detail-row description">
                <strong>Description:</strong>
                <div class="description-text">{{{item.system.description.value}}}</div>
              </div>
              {{/if}}
              
              {{#if item.system.effect}}
              <div class="detail-row"><strong>Effect:</strong> {{item.system.effect}}</div>
              {{/if}}
              
              {{#if item.system.savingThrow}}
              <div class="detail-row"><strong>Saving Throw:</strong> {{item.system.savingThrow}}</div>
              {{/if}}
              
              {{#if item.system.successScaling}}
              <div class="detail-row"><strong>Success Scaling:</strong> {{item.system.successScaling}}</div>
              {{/if}}
            </div>
          </div>
        </li>
        {{/each}}
      </ol>
    </div>
    {{/if}}

    {{!-- Features Tab --}}
    <div class="tab features" data-group="primary" data-tab="features">
      <div class="features-layout">
        {{!-- Feats --}}
        <div class="feature-section">
          <div class="inventory-header">
            <h3>Feats</h3>
            <button type="button" data-action="openCompendium" data-type="feat" title="Add from Compendium">
              <i class="fas fa-atlas"></i> Add Feat
            </button>
          </div>
          <ol class="items-list feat-list">
            {{#each feats as |item id|}}
            <li class="item flexrow" data-item-id="{{item._id}}">
              <div class="item-image">
                <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
              </div>
              <h4 class="item-name">{{item.name}}</h4>
              <div class="item-controls">
                <a class="item-control" data-action="itemView" title="View"><i class="fas fa-eye"></i></a>
                <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
                <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
              </div>
            </li>
            {{/each}}
          </ol>
        </div>

        {{!-- Traits --}}
        <div class="feature-section">
          <div class="inventory-header">
            <h3>Traits</h3>
            <button type="button" data-action="openCompendium" data-type="trait" title="Add from Compendium">
              <i class="fas fa-atlas"></i> Add Trait
            </button>
          </div>
          <ol class="items-list trait-list">
            {{#each traits as |item id|}}
            <li class="item flexrow" data-item-id="{{item._id}}">
              <div class="item-image">
                <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
              </div>
              <h4 class="item-name">{{item.name}}</h4>
              <div class="item-controls">
                <a class="item-control" data-action="itemView" title="View"><i class="fas fa-eye"></i></a>
                <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
                <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
              </div>
            </li>
            {{/each}}
          </ol>
        </div>

        {{!-- Flaws --}}
        <div class="feature-section">
          <div class="inventory-header">
            <h3>Flaws</h3>
            <button type="button" data-action="openCompendium" data-type="flaw" title="Add from Compendium">
              <i class="fas fa-atlas"></i> Add Flaw
            </button>
          </div>
          <ol class="items-list flaw-list">
            {{#each flaws as |item id|}}
            <li class="item flexrow" data-item-id="{{item._id}}">
              <div class="item-image">
                <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
              </div>
              <h4 class="item-name">{{item.name}}</h4>
              <div class="item-controls">
                <a class="item-control" data-action="itemView" title="View"><i class="fas fa-eye"></i></a>
                <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
                <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
              </div>
            </li>
            {{/each}}
          </ol>
        </div>
      </div>
    </div>

    {{!-- Effects Tab --}}
    <div class="tab effects" data-group="primary" data-tab="effects">
      <div class="conditions-section">
        <div class="inventory-header">
          <h3>Conditions</h3>
          <button type="button" data-action="openCompendium" data-type="condition" title="Add Condition">
            <i class="fas fa-plus"></i> Add Condition
          </button>
        </div>

        {{#if (hasItems conditions)}}
        <div class="conditions-list">
          {{#each conditions as |item id|}}
          <div class="condition-item item" data-item-id="{{item._id}}">
            <img class="condition-icon" src="{{item.img}}" title="{{item.name}}"/>
            <span class="condition-name">{{item.name}}</span>
            {{#if item.system.severity}}
            <span class="condition-severity">{{capitalize item.system.severity}}</span>
            {{/if}}
            <div class="item-controls">
              <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
              <button type="button" data-action="conditionRemove" data-condition-name="{{item.name}}" title="Remove Condition">
                <i class="fas fa-times"></i>
              </button>
            </div>
          </div>
          {{/each}}
        </div>
        {{else}}
        <p class="no-items">No active conditions.</p>
        {{/if}}
      </div>
    </div>

    {{!-- Equipment Tab --}}
    <div class="tab equipment" data-group="primary" data-tab="equipment">
      {{!-- Armor Section --}}
      <div class="inventory-header">
        <h3>Armor</h3>
        <button type="button" data-action="openCompendium" data-type="armor" title="Add from Compendium">
          <i class="fas fa-atlas"></i> Add Armor
        </button>
      </div>
      <ol class="items-list armor-list">
        <li class="items-header flexrow">
          <div class="item-name">Name</div>
          <div class="item-detail">DR</div>
          <div class="item-detail">Penalty</div>
          <div class="item-controls"></div>
        </li>
        {{#each armor as |item id|}}
        <li class="item flexrow" data-item-id="{{item._id}}">
          <div class="item-image">
            <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
          </div>
          <h4 class="item-name">{{item.name}}</h4>
          <div class="item-detail">
            {{#if item.system.dr.slashing}}
              S:{{item.system.dr.slashing}} P:{{item.system.dr.piercing}} B:{{item.system.dr.bludgeoning}}
            {{else}}
              {{item.system.dr.value}}
            {{/if}}
          </div>
          <div class="item-detail">{{item.system.stealthPenalty}}</div>
          <div class="item-controls">
            <a class="item-control {{#if item.system.equipped}}equipped{{/if}}" data-action="itemEquip" title="{{#if item.system.equipped}}Equipped{{else}}Not Equipped{{/if}}">
              <i class="fas fa-shield-alt"></i>
            </a>
            <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
            <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
          </div>
        </li>
        {{/each}}
      </ol>

      {{!-- Other Equipment Section --}}
      <div class="inventory-header">
        <h3>Equipment</h3>
        <button type="button" data-action="openCompendium" data-type="equipment" title="Add from Compendium">
          <i class="fas fa-atlas"></i> Add Equipment
        </button>
      </div>
      <ol class="items-list equipment-list">
        <li class="items-header flexrow">
          <div class="item-name">Name</div>
          <div class="item-detail">Qty</div>
          <div class="item-controls"></div>
        </li>
        {{#each equipment as |item id|}}
        <li class="item flexrow" data-item-id="{{item._id}}">
          <div class="item-image">
            <img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/>
          </div>
          <h4 class="item-name">{{item.name}}</h4>
          <div class="item-detail">
            <input type="number" name="items.{{item._id}}.system.quantity" value="{{item.system.quantity}}" data-dtype="Number" min="0"/>
          </div>
          <div class="item-controls">
            <a class="item-control" data-action="itemChat" title="Post to Chat"><i class="fas fa-comment"></i></a>
            <a class="item-control" data-action="itemEdit" title="Edit"><i class="fas fa-edit"></i></a>
            <a class="item-control" data-action="itemDelete" title="Delete"><i class="fas fa-trash"></i></a>
          </div>
        </li>
        {{/each}}
      </ol>
    </div>

    {{!-- Biography Tab --}}
    <div class="tab biography" data-group="primary" data-tab="biography">
      {{editor enrichedBiography target="system.biography.value" button=true owner=owner editable=editable}}
    </div>
  </section>
</form>
